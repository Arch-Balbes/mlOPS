# Copy to .env and adjust if IPv6 changes

# Public ETA API (IPv6), external clients
ETA_PUBLIC_BASE_URL=http://[2a00:1370:8184:1c5d:e61b:c8fa:a5ca:aed5]:8000

# Local / Docker internal
DATABASE_URL=postgresql://eta:eta@localhost:5432/eta_db
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=eta_delivery
MLFLOW_REGISTERED_MODEL=eta_model

CANARY_WEIGHT=0.05
MAE_GATE_RATIO=1.05

# Model paths (defaults work after pipeline)
# MODEL_URI=models/deploy/production.pkl
# CANARY_MODEL_URI=models/deploy/canary.pkl
