# Dev/student stack: Grafana behind tunnel4 / fxTunnel / ngrok (fixes 403 on /login and /api/ds/query).
[security]
csrf_disabled = true

[auth]
disable_login_form = true

[auth.anonymous]
enabled = true
org_role = Admin

[server]
enforce_domain = false
